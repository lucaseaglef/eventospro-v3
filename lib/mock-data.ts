/**
 * @deprecated ARQUIVO REMOVIDO - Dados mockados não devem ser usados em produção
 *
 * Este arquivo foi completamente removido da aplicação de produção.
 *
 * Para desenvolvimento e testes, use:
 * - lib/types.ts para interfaces TypeScript
 * - lib/api-hooks.ts para chamadas de API reais
 * - Dados de teste devem vir de APIs de desenvolvimento ou fixtures de teste
 *
 * Se você está vendo este arquivo, significa que há imports não removidos.
 * Procure por imports de './mock-data' e substitua por hooks de API apropriados.
 */

console.error(
  "🚨 ERRO: lib/mock-data.ts foi removido da aplicação de produção. " +
    "Use lib/api-hooks.ts para buscar dados reais da API.",
)

// Arquivo completamente removido - não deve ser usado em produção
export {}
